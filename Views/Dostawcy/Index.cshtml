@model IEnumerable<Projekt2.Models.Dostawcy>

@{
    ViewData["Title"] = "Dostawcy";
}

<h1>Dostawcy</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Dodaj nowego dostawcę</a>
</p>
<p>
    <a class="btn btn-primary" asp-action="Statystyki">Pokaż statystyki dostawców</a>
</p>
@{
    int totalPages = (int)ViewBag.TotalPages;
    int currentPage = (int)ViewBag.CurrentPage;
    int visiblePages = 5;
    int startPage = Math.Max(1, currentPage - visiblePages / 2);
    int endPage = Math.Min(totalPages, startPage + visiblePages - 1);

    if (endPage - startPage + 1 < visiblePages && startPage > 1)
    {
        startPage = Math.Max(1, endPage - visiblePages + 1);
    }
}

<nav class="text-end flex-column">
    @if (startPage > 1)
    {
        <a class="link-info " href="@Url.Action("Index",  new { page = 1 })">1</a>
    }
    @if (currentPage > 1)
    {
        <a class="link-info text-decoration-none" href="@Url.Action("Index", new { page = currentPage - 1 })">&lt;</a>
    }

    @for (int i = startPage; i <= endPage; i++)
    {
        if (i == currentPage)
        {
            <a class="link-info cur_page" href="@Url.Action("Index", new { page = i })">@i</a>
        }
        else
        {
            <a class="link-info" href="@Url.Action("Index", new { page = i })">@i</a>
        }
    }

    @if (currentPage < totalPages)
    {
        <a class="link-info text-decoration-none" href="@Url.Action("Index", new { page = currentPage + 1 })">&gt;</a>
    }
    @if (endPage <= totalPages - 1)
    {
        <a class="link-info" href="@Url.Action("Index", new { page = totalPages })">@totalPages</a>
    }
</nav>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Surname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Addres)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nr_tel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ilosc_ha_pola)
            </th>
            <th>
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr onclick="location.href='@Url.Action("Details", "Dostawcy", new { id = item.Id })'" style="cursor:pointer;">
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Addres)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nr_tel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ilosc_ha_pola)
            </td>
            <td>
                <a class="btn btn-secondary" asp-action="Edit" asp-route-id="@item.Id">Edytuj</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Usuń</a>
            </td>
        </tr>
}
    </tbody>
</table>
<nav class="text-center flex-column">
    @if (startPage > 1)
    {
        <a class="link-info " href="@Url.Action("Index",  new { page = 1 })">1</a>
    }
    @if (currentPage > 1)
    {
        <a class="link-info text-decoration-none" href="@Url.Action("Index", new { page = currentPage - 1 })">&lt;</a>
    }

    @for (int i = startPage; i <= endPage; i++)
    {
        if (i == currentPage)
        {
            <a class="link-info cur_page" href="@Url.Action("Index", new { page = i })">@i</a>
        }
        else
        {
            <a class="link-info" href="@Url.Action("Index", new { page = i })">@i</a>
        }
    }

    @if (currentPage < totalPages)
    {
        <a class="link-info text-decoration-none" href="@Url.Action("Index", new { page = currentPage + 1 })">&gt;</a>
    }
    @if (endPage <= totalPages - 1)
    {
        <a class="link-info" href="@Url.Action("Index", new { page = totalPages })">@totalPages</a>
    }
</nav>

